#include <iostream>

/**
 * main: entry point for simple calculator
 * Return: 0 on success
 */

int main()
{
	return 0;
}

int add(int x, int y)
{
	return (x + y);
}

int sub(int x, int y)
{
	return (x - y);
}

int mul(int x, int y)
{
	return (x * y);
}

int div(int x, int y)
{
	return (x / y);
}
